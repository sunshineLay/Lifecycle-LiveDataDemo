dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven{ url 'http://127.0.0.1:8081/artifactory/android_group/' }
    }
}
rootProject.name = "LifecyclerDemo"
include ':app'
